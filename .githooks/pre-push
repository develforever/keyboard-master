#!/bin/sh
# Pełna weryfikacja przed wypchnięciem — ten sam zestaw, który uruchamia CI.
# Pominięcie: git push --no-verify

set -e

cd "$(dirname "$0")/.."

if [ ! -d node_modules ]; then
  echo "pre-push: brak node_modules — uruchom 'npm ci'." >&2
  exit 1
fi

npm run verify
